<header>
    <a href="{{ route('home') }}" class="logo"><i class="fab fa-whatsapp"></i> DivulgaZAP</a>
    <div class="nav-actions">
        <a href="{{ route('groups.categories') }}" class="btn btn-outline">Categories</a>
        <a href="{{ route('groups.create') }}" class="btn btn-primary">Share Group</a>
    </div>
</header>